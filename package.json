{
  "name": "risa-ai-assistant",
  "version": "1.0.0",
  "description": "Risa Assistant AI - Asisten Cerdas Rivaldo",
  "scripts": {
    "dev:react": "cd rapi && npm run dev",
    "dev:server": "node server.js",
    "build": "cd rapi && npm run build",
    "start": "node server.js",
    "install:all": "cd rapi && npm install && cd .. && npm install"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}